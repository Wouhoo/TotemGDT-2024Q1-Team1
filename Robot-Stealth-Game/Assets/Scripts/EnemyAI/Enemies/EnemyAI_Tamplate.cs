using System;
using UnityEngine;
using UnityEngine.AI;

public class EnemyAI_TEMPLATE : EnemyAI
{
    // private EnemyVisionManager visionManager;
    private void Awake()
    {
        // SensorManagers
        // visionManager = new EnemyVisionManager(transform, 2, 2);

        // STATES

        // TRANSITIONS
        // At(from, to, condition);
        // Any(to, condition);

        // START STATE
        // stateMachine.SetState(TEXTHERE);

        // CONDITIONS
        // Func<bool> foo() => 
    }
}